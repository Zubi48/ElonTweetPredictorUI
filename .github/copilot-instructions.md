# Copilot Instructions

## Project Guidelines
- For this project, deployment pipeline should deploy only the .NET UI container; the Python predictor is deployed separately. UI must still mount shared data volume to read predictor logs.